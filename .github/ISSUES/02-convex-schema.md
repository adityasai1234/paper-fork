ALL tables day 1: audits, agentOutputs, reports, userRequests, sessions, memories, cronJobs, githubOutputs.

## Acceptance criteria

- [x] `convex/schema.ts` with all tables
- [x] `createAudit` mutation schedules parallel agent actions
- [x] `logSessionEvent()` helper in audits.ts
- [x] `tryScheduleJudge` when all 3 chips complete

## Labels

`buildathon`, `layer-foundation`, `partner-convex`
