Publish README (0 emojis) as first commit. Issue index links to all issues. `.gitignore` included.

## Acceptance criteria

- [x] README at repo root with north star, stack, env vars, issue index, demo script
- [x] Zero emojis in README
- [x] `.gitignore` for node_modules, .env, .convex

## Labels

`buildathon`, `layer-foundation`
