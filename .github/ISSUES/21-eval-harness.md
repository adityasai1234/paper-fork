packages/eval fixture rubric. pnpm eval CI gate.

## Acceptance criteria

- [x] `packages/eval/` with fixture rubric
- [x] `pnpm eval` passes on seeded fixture (3+ FORKED)
- [x] GitHub Actions workflow runs eval on push

## Labels

`buildathon`, `layer-ship`
