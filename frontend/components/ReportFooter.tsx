export function ReportFooter({ className }: { className?: string }) {
  return (
    <footer className={className}>
      <p>paperfork@getkarpathy.com</p>
      <p>Paperfork — find where the paper forked from the repo.</p>
    </footer>
  );
}
